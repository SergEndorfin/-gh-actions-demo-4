{
  "name": "deploy-s3",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild main.js --bundle --platform=node --target=node24 --outfile=dist/index.js --format=cjs"
  },
  "type": "commonjs",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
